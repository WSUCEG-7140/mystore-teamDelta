{
  "short_name": "React App", // Short name of the PWA, often used for displaying app names in limited space.
  "name": "Create React App Sample", // Full name or title of the PWA.
  "icons": [ // Array of icons representing the PWA in different sizes and formats.
    {
      "src": "favicon.ico", // URL or file path of the icon in the .ico format.
      "sizes": "64x64 32x32 24x24 16x16", // Sizes of the icon in pixels, separated by spaces.
      "type": "image/x-icon" // MIME type of the icon image.
    },
    {
      "src": "logo192.png", // URL or file path of the icon in the .png format.
      "type": "image/png", // MIME type of the icon image.
      "sizes": "192x192" // Size of the icon in pixels.
    },
    {
      "src": "logo512.png", // URL or file path of the icon in the .png format.
      "type": "image/png", // MIME type of the icon image.
      "sizes": "512x512" // Size of the icon in pixels.
    }
  ],
  "start_url": ".", // URL to load when the PWA is launched.
  "display": "standalone", // Defines how the PWA should be displayed (standalone, fullscreen, minimal-ui, browser).
  "theme_color": "#000000", // Defines the theme color for the PWA's UI elements.
  "background_color": "#ffffff" // Defines the background color for the PWA's UI.
}
